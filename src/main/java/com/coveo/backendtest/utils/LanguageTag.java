package com.coveo.backendtest.utils;

/**
 * Language tags used to mark which language a search result belongs to.
 *
 * Currently only support French/English (official city name and ascii names)  and others (the alternative names in the database).
 */

public enum LanguageTag {
   FREandENG,
   others;
}
